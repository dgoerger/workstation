### THIS FILE IS MANAGED BY CHEF ###

[domain/default]
autofs_provider = ldap
id_provider = ldap
krb5_store_password_if_offline = False
ldap_uri = ldaps://<%= node['workstation']['_sssd']['ldap_server'] %>/
chpass_provider = krb5
ldap_search_base = <%= node['workstation']['_sssd']['ldap_dc'] %>
cache_credentials = False
auth_provider = krb5
krb5_server = <%= node['workstation']['_sssd']['kerberos_server'] %>
krb5_realm = <%= node['workstation']['_sssd']['kerberos_realm'] %>
ldap_tls_cacertdir = /etc/openldap/certs
ldap_tls_reqcert = allow
# in case users try to log in with their username in all-caps
case_sensitive = false

[sssd]
services = pam, autofs, nss
domains = default

[nss]

[pam]

[sudo]

[autofs]

[ssh]

[pac]

[ifp]
